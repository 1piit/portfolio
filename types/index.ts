export type WorkExperienceType = {
  date: string;
  title: string;
  description: JSX.Element;
  skills: string[];
};

export type ProjectType = {
  title: string;
  image: string;
  description: JSX.Element;
  skills: string[];
};
